;; Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
;; SPDX-License-Identifier: BSD-3-Clause-Clear.

(in .rcsysntpd

(block rcsysntpdtmp

	;; policy

	(blockinherit .agent.base_template))

	(allow subj self (capability (dac_read_search)))

	(allow subj self create_unix_stream_stream_socket)
	(allow subj .sys.fs (filesystem (getattr)))
	(allow subj .filesystems.procfile (file (open read)))

	(call .uci.search_conffile_dirs (subj))
	(call .uci.read_conffile_files (subj))
	(call .uci.search_tmpfile_dirs (subj))

	(dontaudit subj self (capability (sys_admin)))
)
