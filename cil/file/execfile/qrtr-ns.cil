;; -*- mode: CIL; fill-column: 79; indent-tabs-mode: nil; -*-


(in .sys
    (call .qrtr-ns.subj_type_transition (subj)))

;;(in .file
;;    (call .qrtr-ns.obj_type_transition_execfile (unconfined.subj_typeattr))
;;    (call .qrtr-ns.obj_type_transition_runtimetmpfile
;;        (unconfined.subj_typeattr)))

(block qrtr-ns

       ;; Contexts
       ;;

	(filecon
		"/usr/bin/qrtr-ns"
		file
		execfile_file_context)

       ;;
       ;; Macros
       ;;

		(macro obj_type_transition_execfile ((type ARG1))
			(call .file.execfile_obj_type_transition
				(ARG1 execfile file "qrtr-ns")))

       ;;
       ;; Policy
       ;;

	(blockinherit .agent.base_template)
	(call .selinux.linked.subj_type (subj))
	;; search in config files is needed
	;;which is done on start /Stop  of services
	(call .file.search_conffile_dirs (subj))
	(allow subj  self  create_qipcrtr_socket)
	(allow subj self (capability (net_admin)))
      ;;(blockinherit .tmpfile.runtime.obj_template)
	)
